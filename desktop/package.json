{
  "name": "recall-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Recall Electron shell — loads the built frontend.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/validate-url.test.js",
    "start": "npm run build && electron .",
    "build:frontend": "npm --prefix ../frontend run build",
    "dist": "npm run build:frontend && npm run build && electron-builder --mac dmg",
    "dist:win": "npm run build:frontend && npm run build && electron-builder --win nsis --x64"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.4.5"
  }
}
